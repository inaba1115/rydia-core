[project]
name = "rydia-core"
dynamic = ["version"]
description = "A Rust-based UGen DSP library for offline audio experimentation, with Python bindings"
readme = "README.md"
authors = [
    { name = "inaba1115", email = "inaba1115@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: Implementation :: CPython",
    "Operating System :: OS Independent",
    "Topic :: Multimedia :: Sound/Audio",
    "Topic :: Software Development :: Libraries",
]

[build-system]
requires = ["maturin>=1.11,<2.0"]
build-backend = "maturin"

[dependency-groups]
dev = [
    "numpy>=2.2.6",
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
    "ruff>=0.14.10",
    "sounddevice>=0.5.3",
    "ty>=0.0.9",
]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
extend-select = ["I", "RUF", "UP", "TID", "C", "SIM", "PTH"]
extend-ignore = ["C901", "F401", "F403", "E722", "RUF007", "E741"]

[tool.pytest.ini_options]
minversion = "9.0"
testpaths = ["tests"]
addopts = [
    "-ra",
    "--strict-markers",
]

[tool.coverage.run]
branch = true
source = ["hyperscore"]
omit = [
    "*/__init__.py",
]

[tool.coverage.report]
show_missing = true
skip_covered = true
precision = 2
